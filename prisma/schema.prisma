//////////////////////////////////////////////////////////////////////////////////////////////
// DO NOT MODIFY THIS FILE                                                                  //
// This file is automatically generated by ZenStack CLI and should not be manually updated. //
//////////////////////////////////////////////////////////////////////////////////////////////

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

enum VehicleStatusEnum {
  AVAILABLE
  UNAVAILABLE
}

/// @@delegate(userType)
/// @@allow('all', true)
model User {
  id                  Int     @id() @default(autoincrement())
  username            String  @unique()
  email               String  @unique()
  password            String
  dni                 String
  firstName           String  @map("first_name")
  lastName            String  @map("last_name")
  phone               String
  isActive            Boolean @default(true)
  userType            String  @map("user_type")
  delegate_aux_client Client?

  @@map("users")
}

/// @@allow('all', true)
model Client {
  id                Int    @id()
  address           String
  delegate_aux_user User   @relation(fields: [id], references: [id], onDelete: Cascade, onUpdate: Cascade)

  @@map("clients")
}

model Vehicle {
  id                 Int               @id() @default(autoincrement())
  plate              String            @unique()
  model              String
  brand              String
  weightCapacity     Decimal           @map("weight_capacity")
  color              String
  km                 Int
  volumeCapacity     Decimal           @map("volume_capacity")
  passengersCapacity Int               @map("passengers_capacity")
  status             VehicleStatusEnum @default(AVAILABLE)

  @@map("vehicles")
}
